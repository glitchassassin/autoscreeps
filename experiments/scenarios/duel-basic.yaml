version: 1
name: duel-basic
description: Run a basic head-to-head experiment on mirrored copies of a random 1x1 sector.
reset: full
mapGenerator:
  type: mirrored-random-1x1
run:
  maxTicks: 200
  pollIntervalMs: 1000
  maxWallClockMs: 300000
  maxStalledPolls: 30
